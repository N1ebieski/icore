$(document).on('readyAndAjax.n1ebieski/icore/web/scripts/plugins/jquery-lazy@init', function () {
    $('.lazy').lazy({
        effect: "fadeIn",
        effectTime: "fast",
        threshold: 0
    });
});
